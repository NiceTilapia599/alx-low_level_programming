#include "main.h"
#include <stdio.h>
/**
 * main - Prints the name of the program
 * @argc: argument count
 * @argv: Array name
 * Return: 0
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
